Data Collection Next Steps
--------------------------
Pre-Images: accessible through Google Earth, NASA space view, or any other commercial satellite views
Post-Images: accessible throughnews sources or StreetMap / more up to daet satellite views

Parameters to ensure standard pre/post imagery:
    Fixed coordinates
    Same zoom level (standard image size, can be split into patches or stretched)
    Vertical top-down view / side view
    Same field of view & crop
    Clean map style (no labels/UI)

Dev Next Steps
--------------

Clean up the script, and decide:

1- When/If switch to and begin training and prepping for an AI model
2- Work further on the current algorithmic approach
    a_ Switch to a "zone of maximum difference" approach rather than "area where there is high difference" approach
        this 'should' help with stark differences in pre / post images


General Next Steps
------------------

1- Damage Detection Prototype
Use the pre/post destruction image pairs to test a basic change detection algorithm.
Make sure the algorithm works independently of the blue rectangles already drawn.
Compare detected areas with the marked ones to evaluate accuracy (IoU, precision, etc.).

2- Height & Surface Area Estimation
Use the existing metadata dataset (height, area, volume) to test simple height and surface area estimation methods.
Try calculating height from shadow length or other cues, and compare with manual data.
Check how well automatic calculations match the dataset numbers.

3- (DONE) Data Organization
-- Clean up and organize the pre/post image files into training and testing folders.
-- Create or update a key file linking coordinates, locations, and image names.

4- Collaboration Setup
Split work
Keep everything versioned in the GitHub repo as we go.

5- Follow-Up
Once both parts are working, combine them to estimate debris volume and mass.
Plan next meeting to show initial detection and calculation results.
